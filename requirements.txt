# Core deep learning
# IMPORTANT: plain `pip install torch` installs the CPU-only build.
# For GPU training on a GTX 1660 Super (CUDA 12.1):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# For CUDA 11.8:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
# Run `nvidia-smi` in a terminal to find your CUDA version.
torch>=2.1.0
transformers>=4.40.0
sentencepiece>=0.2.0
accelerate>=0.30.0

# Data
pandas>=2.0.0
numpy>=1.24.0
langdetect>=1.0.9

# Notebook + progress
jupyter>=1.0.0
ipywidgets>=8.0.0
tqdm>=4.66.0

# Web app
flask>=3.0.0

# Section 7 only -- Keras .fit() from-scratch comparison.
# CPU build is plenty for the small char-level models in that section.
tensorflow>=2.16
